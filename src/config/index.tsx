import { Ienvierment } from '../model/envierment.tsx';

const config: Ienvierment = {
    api: {
        usersDataURL: 'https://jsonplaceholder.typicode.com/users',
        formPostsURL: 'https://jsonplaceholder.typicode.com/posts',
    },
};

export default config;
